apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nominatim-pvc
  namespace: osm
spec:
  accessModes: [ReadWriteOnce]
  storageClassName: local-path
  resources:
    requests:
      storage: 300Gi
