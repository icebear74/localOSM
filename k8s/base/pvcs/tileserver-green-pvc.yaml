apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: tileserver-green-pvc
  namespace: osm
  labels:
    app: tileserver
    color: green
    active: "true"
spec:
  accessModes: [ReadWriteOnce]
  storageClassName: local-path
  resources:
    requests:
      storage: 200Gi
