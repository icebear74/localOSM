apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nominatim-green-pvc
  namespace: osm
  labels:
    app: nominatim
    color: green
    active: "true"
spec:
  accessModes: [ReadWriteOnce]
  storageClassName: local-path
  resources:
    requests:
      storage: 300Gi
