apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nominatim-blue-pvc
  namespace: osm
  labels:
    app: nominatim
    color: blue
    active: "false"
spec:
  accessModes: [ReadWriteOnce]
  storageClassName: local-path
  resources:
    requests:
      storage: 300Gi
