apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: tileserver-pvc
  namespace: osm
spec:
  accessModes: [ReadWriteOnce]
  storageClassName: local-path
  resources:
    requests:
      storage: 200Gi
