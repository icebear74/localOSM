apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: photon-blue-pvc
  namespace: osm
  labels:
    app: photon
    color: blue
    active: "false"
spec:
  accessModes: [ReadWriteOnce]
  storageClassName: local-path
  resources:
    requests:
      storage: 80Gi
