apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: valhalla-pvc
  namespace: osm
spec:
  accessModes: [ReadWriteOnce]
  storageClassName: local-path
  resources:
    requests:
      storage: 100Gi
