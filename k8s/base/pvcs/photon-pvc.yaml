apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: photon-pvc
  namespace: osm
spec:
  accessModes: [ReadWriteOnce]
  storageClassName: local-path
  resources:
    requests:
      storage: 80Gi
