apiVersion: v1
kind: Service
metadata:
  name: valhalla
  namespace: osm
  labels:
    app: valhalla
spec:
  type: NodePort
  selector:
    app: valhalla
    active: "true"
  ports:
    - name: http
      port: 8002
      targetPort: 8002
      nodePort: 31088
