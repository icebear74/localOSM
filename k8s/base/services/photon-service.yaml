apiVersion: v1
kind: Service
metadata:
  name: photon
  namespace: osm
  labels:
    app: photon
spec:
  type: NodePort
  selector:
    app: photon
    active: "true"
  ports:
    - name: http
      port: 2322
      targetPort: 2322
      nodePort: 31091
