apiVersion: v1
kind: Service
metadata:
  name: photon-blue
  namespace: osm
  labels:
    app: photon
    color: blue
spec:
  type: NodePort
  selector:
    app: photon
    color: blue
  ports:
    - name: http
      port: 2322
      targetPort: 2322
      nodePort: 31092
