{
  "_doc": "Demo config: keep routing parameters at defaults, only define service URLs and optimization sweep.",

  "photon_base_url": "https://photon.optadata.io",
  "nominatim_base_url": "https://nominatim.optadata.io",
  "valhalla_base_url": "https://valhalla.optadata.io",

  "optimize": {
    "enabled": true,
    "costing_options_variants": [
      {
        "auto": {
          "use_distance": { "from": 0.0, "to": 1.0, "step": 0.5 },
          "maneuver_penalty": { "from": 10, "to": 100, "step": 20 }
        }
      }
    ]
  }
}
